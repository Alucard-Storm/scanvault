import 'package:flutter/material.dart';

/// App color constants
class AppColors {
  AppColors._();

  // Primary colors
  static const Color primarySeed = Color(0xFF00897B);
  static const Color secondaryAccent = Color(0xFF7C4DFF);

  // Surface colors
  static const Color surfaceLight = Color(0xFFFAFAFA);
  static const Color surfaceDark = Color(0xFF1C1C1E);

  // Document enhancement colors
  static const Color scanOverlay = Color(0x8000897B);
  static const Color edgeHighlight = Color(0xFF00E676);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFF44336);
}
